import 'package:flutter/material.dart';

class AllTenderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('All Tender Page Content'),
    );
  }
}
