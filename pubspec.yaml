name: live_tender_bd_admin
description: "live_tender_bd_admin"
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_svg: ^1.0.0
  get: ^4.6.5
  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.5
  uuid: ^4.4.0
  fluttertoast: ^8.2.5
  provider: ^6.0.0
  path_provider: ^2.0.11
  firebase_auth: ^4.6.1
  shared_preferences: ^2.2.3
  dropdown_search: ^5.0.2
  syncfusion_flutter_datagrid: ^20.3.46
  excel: ^2.0.6
  permission_handler: ^11.3.1
  platform: ^3.1.5
  path: ^1.9.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/bg.jpg
